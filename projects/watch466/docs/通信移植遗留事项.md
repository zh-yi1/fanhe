# 通信栈移植遗留事项

> 2026-07-29 整理。elunchbox → fanhe(rebuid_agreement) 通信栈移植已全部完成
> (BLE 链路 / UART 链路 / 桥接转发 / 本地业务 / 主MCU OTA / 加热模块 OTA),
> 代码位于 `functions/comm/`(lb_ 前缀 18 个文件)。以下是未完成 / 待验证项。

---

## 1. 必须做(否则跑不起来)

### 1.1 目标编译验证(已通过)
- 2026-07-30 Windows 侧 RV32 交叉工具链编译通过,0 error。遗留警告见 §2.5。
- **`app.cbp` 会被从 `new` 分支的合并冲掉** —— `f9c4350` 那次合并把
  `functions/comm` 的 include 目录和 18 个 Unit 全清了,导致 `func.h` 找不到
  `lb_proto.h`。以后每次从 `new` 合过来,先
  `grep functions/comm projects/watch466/app.cbp` 看一眼,没有就是又丢了。
- **`func_black_screen_page.c` 曾不在 `app.cbp` 里**(2026-07-30 发现):链接到
  `func_watch_stubs.c` 的空桩,切到 `FUNC_BLACK_SCREEN` 后 `func_run` 空转
  喂不了狗 → WDT 复位。已加入工程并删桩。**新增页面文件后务必检查 `app.cbp`**,
  桩文件会把"没编译进来"掩盖成运行期崩溃。
- `functions/new_ui/ui.h` 曾被 `.gitignore` 的 `**/ui.h` 规则吞掉(定义 `ui_sys_t`
  的头文件进不了仓库),后来改名为 `app_ui.h` 绕开。新增 `*ui.h` 命名的头文件要留意。
  (注:`projects/watch466/ui.h` 是资源工具自动生成的那个,守卫 `_UI_H`,同样不进仓库,
  各人本地一份 —— 和 `app_ui.h` 是两个完全不同的文件。)
- **`app_ui.h` 不要往里加移植层的声明** —— 2026-07-30 有人拉代码后报
  `new_ui_appointment_take/clear` 隐式声明,原因是他本地的 `app_ui.h` 还是
  `507251e` 那版(有 `ui_sys_t`、有 `lb_ui_sync_pull`,缺预约那三条),
  大概率是自己从 UI 端拿 `ui.h` 手动改名覆盖上去的。
  该文件由 UI 端整份重发,加什么都会被冲掉。
  已把 `lb_ui_sync_pull()` 和 `new_ui_appointment_*()` 四条声明迁到
  `general_ui.h`(它 include 了 `app_ui.h`,页面照旧只 include `general_ui.h`),
  `app_ui.h` 恢复成纯 UI 结构体。守卫也从 `__NEW_UI_H__` 改成 `__APP_UI_H__`。
  下次遇到类似报错先
  `git status projects/watch466/functions/new_ui/app_ui.h` 看有没有本地改动。

### 1.2 蓝牙/串口联调(进行中)
基本链路自检点:

| 环节 | 位置 | 检查方法 |
|:---|:---|:---|
| UART1 初始化 | `main.c` → `lunchbox_uart_init(LB_BAUD)` | 115200 8N1, TX=PB8, RX=PB9 |
| 串口收包 | `lb_uart_link.c` ISR → 环形缓冲 | `lb_link_rx_overflow()` 应恒为 0 |
| 串口解析 | `func.c` 主循环 → `lunchbox_uart_process()` | 收到帧有日志打印 |
| 心跳 | 模块发 0x05 → 自动应答 | 最简单的通路验证 |
| BLE 收包 | `app_blue_fit.c` 4 槽环形缓冲 → `lunchbox_ble_process()` | UUID ae30/ae03/ae04 |
| BLE 应答 | APP 发 0x01 查产品信息 | 会先透传模块查版本号,模块无应答则 1s 超时后仍回 APP |
| 桥接转发 | APP 发 0x04 控制 → UART 0x01 | 一发一收队列,100ms×3 重试 |

### 1.3 上板验证结果（2026-07-29 真机联调）

移植时做的设计假设,已逐条实测:

| # | 假设 | 结果 |
|:--:|:---|:---|
| 1 | 模式预设同步(DP2+DP7+DP5 不带 DP10)不会误触发加热 | ✅ 模块只更新预设, `DP10=OFF` |
| 2 | 模块每分钟经 dpid=14 推 RTC 时间 | ✅ 间隔正好 60 秒 |
| 3 | 时间同步链: 模块应答后才保存本机时间 | ✅ 模块有应答, 链路成立(但时间值有偏移, 见 §1.4) |
| 4 | 0x08 删除只发 2 字节 `[mode,id]` | ❌ 模块回 err=01, **已改为发完整 42 字节** |
| 5 | 0x07/0x08 的 BLE 应答带 1 字节数据(文档写无数据) | ✅ 模块确实会回, 沿用 |
| 6 | 加热模块 OTA 断电续传 | ⬜ 未测, 需固件包 |

其余已验证通过: BLE 收发/组帧/分发/应答、串口收发、心跳自动应答、桥接完整转发、
加热启停、产品信息(81B + 模块版本号回填)、预约新增/修改/查询(含多帧列表)、模式预设查改。

### 1.4 加热模块固件待修（已反馈,非本机代码问题）

1. **RTC 时间偏移**:主板下发 dpid=11 后,模块存进 RTC 的值比下发值大。
   早期固件实测 **+57600 秒(16 小时)**;2026-07-31 模块 v034 实测变为
   **+86400 秒(整 1 天)** —— 同步应答里**原样回显**下发值(假象),
   几秒后模块自己的整分推送就带着 +1 天(铁证:同一日志内
   下发 1785468530 → 4 秒后推 1785554934,差 86404)。
   时分秒不差、日期多一天,板子屏幕只显示时分所以不易察觉。
   模块侧需查内部日历/天数换算。
2. **预约时间丢日期**:下发预约 time=1785317795(完整 unix 秒),读回来变成
   **34595**,即 `unix % 86400`(当天秒数)。协议字段写的是 unix 秒。
   与第 1 条不一致(RTC 加偏移、预约取余),模块内部时间处理需统一。
   **2026-07-31 补充 —— 到点不触发的完整定性(时区差 8 小时)**:
   实测 APP 自己发的就是**本地天内秒**(16:03 → 57780,BLE 原始帧实锤,
   不是模块截的),而模块 RTC 走 UTC unix,比较用的天内秒是 UTC 的
   (本地 16:03 时 RTC%86400 = 08:03)→ 两边永远差时区,要到本地凌晨才碰上。
   另 APP 建"仅一次"预约 repeat 发 **0xFF(每天重复)**,语义也错 ——
   就算触发了也不会被消费,会天天到点烧一次。这条板子救不了,须 APP 改。
   板子已打桥层补丁兜触发(见 §2.4 APP 违例清单),模块侧仍需统一时间处理。
3. **每条命令回两遍**:同一帧连发两次,主板第一条当应答、第二条当主动上报,
   导致 APP 每次操作收到"命令应答 + 状态推送"两条相同内容。需告知 APP 端。
4. **插电唤醒进黑屏充电页 —— 已修**(2026-07-31 主板侧落地,方案比原设想①
   更准:不用 5s 观察窗)。修法:`lb_boot_lid_wait()` 开机等首帧本来就阻塞
   ≤3s,首帧 DP4 报充电(1/2)且无加热任务 → `lb_heat_cmd_power(false)`
   把刚拍开的模块拍回去 → 落黑屏充电页;有加热任务仍优先盖盖弹窗。
   遗留风险:模块充电检测抖动(Charge 1→0)会让黑屏页被"拔线"逻辑带出去
   真关机,等模块加消抖。原始现象记录留档如下:
   (2026-07-30 二次实测)。
   实测(带括号打印固件,两次复现一致):深睡中插电 → 主板 **`WKUP reset`
   冷启动**(ROM 横幅确认,不是 WDT/软复位;RAM 全清,醒因闩锁/唤醒观察窗
   都不存在了)→ `func_run` 时 `CHARGE_DC_IN()` 未就绪没进黑屏页 → 正常
   开机:进主页 + 自动 power_on 把模块拍开。模块在 power_on 应答里报了
   DP4=1,但冷启动路径无人消费。
   **结论:模块侧发什么指令都救不了这条路** —— 板子是被复位重启的,不是
   深睡里被串口叫醒的,`lb_wake_apply()` 那套(含醒后主动查询)全不参与。
   要修只能走主板方案①:**开机后短观察窗(约 5s),`charging_now()`(含
   DP4)命中且无用户操作 → 关模块 → 落黑屏页**,顺带覆盖"冷启动插线但
   DC 检测未就绪"。方案②(`is_guioff` 守卫)针对的"唤醒建 form 撞 GPU"
   与本场景无关,仍备查。
   另:一次实测中模块 power_on 应答报 Charge=1,2s 后主动上报又变 0
   (若非"插了就拔"就是模块充电检测抖动)—— 做方案①前需模块侧确认,
   判据抖动会导致黑屏页进错/漏进。

---

## 2. UI 对接(接口已备好,等页面来调)

| 事项 | 接口 |
|:---|:---|
| 页面读设备状态 | `lb_ui_state_get()`,按 `seq` 判断是否需要刷新 |
| 页面读预约列表 | `lb_ui_schedules_get()`,判断条件必须是 `complete && seq 变化`(传输中 seq 会连跳、count 从 0 递增);进页面调一次 `lb_ui_schedules_refresh()`;转圈/失败提示查 `lb_ui_schedules_sync_state()`。增删改后的重查由通信层自动做 |
| 页面操作下发 | `lb_heat_cmd_start/stop/power/schedule_set/...` |
| 自动跳页 | `lb_ui_route_poll()` 已接入 —— 唯一调用点是 `func.c` 的 `lb_ui_route_apply()`,在 `func_process()` 里每轮跑。切换动画中/OTA 中跳过且不消费边沿。**别在其他地方调**,边沿只能被消费一次。**加热自然结束 → 保温页**(保温页 enter 自动下发 194F/24h);主动停止(本机 stop/heat_off、APP 0x04 带 DP10=0,均已调 `lb_ui_heat_stop_expected()`)→ 回首页。DP6 区分不了自然/主动停止(模块两种都清 0),靠发停止的人打招呼。**模块报故障(DP9≠0 且非低电)→ 停止加热 + 回首页**(2026-07-31 加):故障边沿置停止标志并返回 HOME,模块还在加热(DP10=1)时补发一条 `lb_heat_cmd_stop()`(关机时序进行中不插队),故障与 DP10=0 同帧时也不会被当成自然结束进保温;低电排除在外,它有独立的 `FUNC_LOWBAT` 页。**充电中保温结束 → 留在保温页**、**拔充电线 → 回首页**(2026-07-31 加):路由基线加了 DP4,保温结束(24h 走完/模块自己停)时若线还插着就返回 NONE 不跳页,拔线边沿(DP4→0)才回首页;加热中(模式1~4且 DP10=1)拔线不动,那是转电池继续加热。拔线边沿只按模块 DP4 判(模块侧已消抖),不掺本机 `CHARGE_DC_IN()`。**加热中插电 → 拔线要续加热**(2026-07-31 补):模块在加热中插电会自动转保温,但那份加热的 DP6 剩余时间还在走。路由记下这次保温是从哪个加热任务转来的(`lb_warm_from_heat`,含模式和转保温前的 DP7 温度 —— 转完 DP7 就变成保温温度了),拔线时 **DP6>0 → 补发 `lb_heat_cmd_start(原模式, 原温度, 剩余分钟)` 并回加热页**;DP6==0 → 加热本来就该结束了,回首页。那条命令必须发:不发模块会一直保温、加热页倒计时是假的,而且保温页 exit 的"还在保温就停保温"会把它掐掉(发了之后镜像预写成加热模式,那条判据不成立)。这套只对"加热中插电"这一条路生效,APP 下发的保温、加热自然结束转的保温都不适用。**充电中人自己选的保温 → 拔线留在保温页**(2026-07-31 补):拔线边沿按"这份保温是哪来的"分三条 —— 加热转来的且 DP6>0 → 续加热回加热页;加热转来的且 DP6==0 → 回首页;**非加热转来的(模式页/APP 下发)且模块还在保温(DP10=1 且 DP2=5)→ 返回 NONE 留在保温页**,等它自己结束或用户退出;其余回首页。判据用进分支前取下的 `was_from_heat`,不能用清零后的标志,否则"加热转来的 + DP6==0"会被误判成手动保温而留在页上 |
| 加热/OTA 期间禁止息屏 | **已接**(2026-07-30):`elunchbox_lp.c` 的 `elunchbox_heating_blocks_idle()` = `lunchbox_heating_task_active() \|\| lb_ota_is_active()`;同时 `elunchbox_guioff_idle_expired()` 也挡一层,否则会"关屏→亮回"每轮闪。挡住时顺手喂满空闲计时,免得加热一结束屏立刻黑。**2026-07-31 加一条**:`func_cb.sta == FUNC_NEW_WARM_PAGE && lunchbox_charging_now()` 也挡 —— 充电中保温结束后页面留在保温界面(见上一行),不挡的话 5 分钟空闲照样走关机时序落到黑屏充电页,"一直保持在保温界面"就只剩 5 分钟。拔线回首页后不再挡,空闲计时从头走 |
| BLE 连接状态图标 | `lb_ui_ble_is_connected()` 或 `lb_ui_state_get()->ble_connected`;连接/断开由 `app_blue_fit.c` 回调调 `lb_ui_ble_link_set()` 写入,变化时 `seq` 自增 |

### 2.1 `g_ui_sys` 数据面(2026-07-29 接入)

新 UI 的 `ui_sys_t g_ui_sys`(`new_ui/app_ui.h`)是页面唯一读的数据结构,
由 `lb_ui_sync_pull()`(实现在 `new_ui/general_ui.c` 末尾)每轮从状态镜像单向刷新,
挂在 `func_process()` 里。页面只读,不要往里写。

映射关系:

| 字段 | 来源 |
|:---|:---|
| `temp` | `lunchbox_temp_idx_to_f(st->heat_temp)`(镜像是档位 0~6,结构体是 °F) |
| `time_min` / `remain_min` | DP5 / DP6 |
| `keep_warm_min` | `heat_duration - remain_time`(保温固定下发 24 小时) |
| `bat_level` | DP3 |
| `lowbat` | **DP9 故障 == `LB_FAULT_LOW_BATTERY`(0x0a)**,不用 DP3 |
| `charging` / `full_charge` | DP4 == 1 / == 2 |
| `bt_linked` | `lb_ui_ble_is_connected()` |
| `hour` / `min` | `lb_get_display_tm()` |
| `lid_open` | **无数据源,恒 false**,见 §2.2 |

### 2.2 预约:屏幕只能新增(设计决定,非缺口)

屏幕侧**只提供新增**,不做列表展示、不做修改、不做删除 —— 改和删只走 APP
(蓝牙 0x07/0x08 经桥转 UART 0x03)。

- 新增路径:预约键 → 预约时间页(时:分:秒滚轮)→ `new_ui_appointment_set()`
  暂存 → 加热设置页选温度/时长 → 确认时 `new_ui_appointment_take()` 命中
  → `lb_heat_cmd_schedule_set()`,回首页。同一个加热设置页,没暂存时走
  `lb_heat_cmd_start()` 立即加热。
- ID 由本机分配:`lb_ui_schedule_alloc_id()` 按列表镜像挑 1~12 最小空闲值,
  所以预约页 `enter()` 里必须先 `lb_ui_schedules_refresh()`。满 12 条丢弃并打日志。
- 下发参数固定 `enabled=1`、`repeat=0`(单次)、`name=NULL`(页面无输入项)。
- `lb_heat_cmd_schedule_delete()` 因此没有本机调用点,保留给将来/调试用。

### 2.3 UI 侧待办 / 待确认

1. **`lid_open` 没有数据源** — 首页"上盖打开"确认框靠这个字段触发。协议属性表
   没有上盖状态,DP9 故障文档写的是 `0=正常 1=故障` 两值,`fault_code 0x05
   上盖 5V 短路` 是故障不是开合状态。**需模块侧加一个上盖状态 DP,或主板本地
   接霍尔/微动自检**。当前恒 false,弹窗不会触发。
2. **DP9 到底回 0/1 还是 fault_code 原值待实测** — `lowbat` 判据依赖模块把
   `fault_code`(§4.1.6)原值放进 DP9。若实测只回 `0/1`,则区分不出低电与高温,
   得改判据或让模块扩展该字段。
3. ~~**低电页是死胡同**~~ — 已确认不用管:低电时加热模块会直接切断主板供电,
   所以进低电页后出不去、串口不跑都不构成问题。
4. **预约执行时 DP2 回什么待实测** — 面板预约灯(LED4)的规则是"列表镜像有
   enabled 预约 → 常亮;执行中 → 灭"(`func_led.c` 的 `led_res_pending()`)。
   "执行中"两层判据兜底:DP2==4(预约) 直接判;加热使能 0→1 时
   `lb_ui_state_feed_dp()` 自动标脏重查列表,单次预约被模块消费后从列表消失,
   灯随之灭。若实测模块执行预约时 DP2 回的是预约里存的模式(非 4),
   靠第二层也能灭,只是晚一次列表往返。

### 2.4 开关机时序(2026-07-30 真机联调通过)

状态机在 `lb_uart_app.c`,一发一等,超时 500ms 也推进。

- **开机**:上电 500ms 后自动 `power_on(DP1=1)` → 应答 → 查预约列表 → IDLE。
- **关机**:`heat_off(DP10=0)` → 应答 → `power_off(DP1=0)` → 应答 → `OFF_DONE`
  → `func.c` 的 `lb_shutdown_seq_apply()` 取走边沿 → **manual_off 超低功耗深睡**
  (和长按 TCH5 3s 落地方式一致,不走 `FUNC_PWROFF` —— 那条路充电时
  `func_pwroff()` 会 return 不断电,会反复进关机页)。
- 入口三个,都汇到 `lunchbox_shutdown_start()`:APP 下发 0x04 带 DP1=0
  (`lb_ble_app.c` 拦截,每步应答各回 APP 一条);本机长按 TCH5 3s
  (`func_key.c`,原来是直接深睡、不通知模块,已改为走时序);
  **5 分钟无操作自动关机**(`func_lowpwr.c` 的 `sleep_process`,2026-07-30 改:
  原来 idle 到点只是息屏、再走"30s + bt_is_allow_sleep"的旧深睡机制,实测被
  模块心跳反复吵醒且被 BT 状态卡住睡不下去;现在直接走关机时序这条已验证的路。
  加热/保温/OTA 中不触发;黑屏充电页不重复触发;BLE 连着但无操作**也会**关机)。
- OTA 中由 `lunchbox_shutdown_blocked()` 拦下,APP 会收到 `ERR_EXEC_FAIL`。
- **充电不拦关机(2026-07-30 改)**:时序照跑(停加热+关模块),落地分流 ——
  未充电 → manual_off 深睡(真关机);充电中 → **黑屏充电页 FUNC_BLACK_SCREEN**,
  与真关机唯一区别是主板不深睡、还收串口状态(模块充电时仍上报 DP4 驱动动画)。
  黑屏页只认开关键短按(补跑 `lunchbox_boot_seq_kick()` 后进主界面),
  拔线去抖 2s 重走关机时序落深睡;强制跳页在该页冻结(边沿留着开机后生效)。
  冷启动时就插着线 → `func_run` 直接进黑屏页,并 `lunchbox_boot_seq_cancel()`
  撤销自动 power_on,模块保持关。
- **深睡唤醒(2026-07-30 二改)**:manual_off 唤醒**不重启**(RAM 原地复活),
  唤醒源只有 PE1(TCH5,持续 2s) 和 PB9(串口)。**任何串口指令都唤醒** ——
  曾做过"睡眠循环里偷听判帧,无关帧继续睡"(`lunchbox_wake_probe`),
  实测睡眠上下文收帧不完整("加热中和充电中指令收不全"),已回退删除。
  醒因闩锁(KEY/UART)在深睡循环置入,醒来后 `func.c` 的 `lb_wake_apply()`
  落页:KEY → `boot_seq_kick()` 进主界面;UART → **主动发状态查询**
  (`lb_heat_cmd_status_query()`,0x01 只带 DP11 时间戳 —— 不带 DP1:
  带 1 会把关着的模块拍开机,带 0 可能停掉预约到点正跑的加热),
  开 3s 观察窗等应答刷进状态镜像后判 —— 充电 → 黑屏充电页,
  加热 → 加热页,都不是(心跳等)→ 留在原页,靠 5min 无操作自动关机
  兜底重新睡回去;窗内 1s 没等到判据再补发一次查询。
  之所以主动查:唤醒那一帧本身常因 UART 起得慢收残,不能靠它判醒因。
  模块侧已确认:不带 DP1 的 0x01 查询帧,模块会**全量字段应答**。
  模块关(DP1=0)后还发不发心跳、充电时会不会主动上报、预约到点还执不执行,
  仍是模块固件行为,待联调确认。
- **盖盖上电"继续加热?"弹窗**(2026-07-30, 三版返工后定型):硬件上
  开盖=整机断电、盖盖=来电冷启动。定型结构(融进启动流程, 无背光补丁):
  ①**判定在 `func_run` 启动段**(与"插线进黑屏页"检查同一位置):
  `lb_boot_lid_wait()` 小循环喂狗跑 `lunchbox_uart_process` 等模块首帧
  (`valid` 置位, 实测 ~600ms; 模块不答 3s 兜底照常开机)。首帧带加热/保温
  (`heat_enable=1`, 含模式 5)且非低电(直接读镜像 fault, g_ui_sys 此时
  还没刷)→ **暂存模式/温度/总时长/剩余时长 → 置 pending, 不停加热**。
  ②**弹窗合成进主页第一帧**:`func_home_page_enter()` 里
  `lb_lid_popup_pending_take()` 命中就地 `func_confirm_overlay_show()`,
  form 首帧渲染就带着弹窗 —— 亮屏即主页+弹窗, 无闪切。
  **开机背光时序**(判定等待期花屏的修复):背光走 **PG_BL_TMR4 PWM**,
  `led_pg_on/off`(即 `LCD_BL_EN/DIS`)对它**完全无效**(api_pwr.h 注明
  "使用PWM后失效"), 上电默认输出就是亮的 —— 所以"别开背光"这个方向
  本身就错了, 必须**主动压灭**:`gui_init` 开头(LCD 上电之前)直接
  `bsp_pwm_duty_set(PORT_TFT_BL, 0, false)` 把占空比写 0。
  ⚠ 不能用 `lcd_drv_set_brightness(0)` 压 —— 驱动里 `last_duty==duty`
  守卫(初始都为 0)会把这次写挡掉, 寄存器根本没动。
  亮屏走 tft 自带 kick 机制:`tft_init` 置 kick → **首帧推完**
  (`tft_frame_end`)→ 再等 3 TE → 主循环 `tft_bglight_frist_set_check`
  写回正常亮度(它先置 last_duty=0 再写, 能穿过守卫)。配套:gui_init
  的 `tft_bglight_open()` 饭盒下跳过、主页 form_create 的 `force_on` 已删。
  深睡唤醒不走这套(`lunchbox_display_on` → force_on 直接写亮度)。
  (前两版教训:判定放主循环、弹窗靠 process 轮询拉起, 和渲染/背光赛跑,
  垒了判定门+三处背光拦截+150ms 延迟亮屏还是闪 —— 时序问题该改流程
  顺序, 不该拿补丁追。)
  ③**弹窗期间不立刻停加热**(容错: 用户觉得没盖好开盖重盖, 再次冷启动时
  模块还在加热, 弹窗还会再来; 一进弹窗就停的话重盖后就不弹了):
  `lb_lid_popup_poll()`(主页 process 弹窗期间每轮调)挂满 **1 分钟**没
  表态才补发 `lb_heat_cmd_stop()`(只发一次)。
  ④结果(`func_home_page.c` process):
  YES → `lb_lid_confirm_yes()`: 没停过 → 模块本来就在跑, 直接跳页
  (保温→保温页, enter 见模块在保温不会重发/清计时; 加热→加热页);
  超时停过 → 先 `lb_heat_cmd_resume(模式,温度,总时长,剩余)`(带 DP5+DP6,
  **模块侧需确认认不认控制方向的 DP6**)续跑再进加热页。
  NO → `lb_lid_confirm_no()` 立即停加热, 留主界面。
  resume 验证法:模块应答恒为全量字段(已确认), 发 resume 后看应答 ——
  DP6=下发的剩余值即认了; DP6 被重置成 DP5 即忽略了, 需模块侧支持。
  弹窗决策期间 `lb_ui_route_apply()` 把边沿取走丢弃(停止产生的回首页边沿、
  续跑产生的加热边沿都不能抢页)。首帧判定天然排除"开机几秒后预约到点
  新起的加热"(那种走正常路由进加热页)。插线冷启动走黑屏页分支不进等待;
  深睡唤醒不重启不过 func_run, 不会再弹。
- **黑屏充电页开机 = 长按 TCH5 3s**(2026-07-30 改,原来短按就开):
  在 `func_key_handle_pwr_long` 统一处理 —— 该页上 3s 到点的语义从"关机"
  换成"开机"(`boot_seq_kick` + 进主界面),页面本身不再处理任何按键。
- **镜像预写(预测-校正)**(2026-07-31 新增):按键"发指令→立刻切页"存在
  空窗 —— 镜像要等模块应答才更新, 新页首屏拿旧数据渲染。机制:
  `lb_heat_cmd.c` 的 start/resume/stop/heat_off/power 发送成功即调
  `lb_ui_state_predict_*()` 把期望结果预写进镜像(seq++ 页面立刻刷新);
  预写**同步路由基线不产生边沿**(路由边沿从此只由模块自发变化产生)、
  **不触发 mark_dirty**、**不置 valid**(开机 power_on 也走预写, 而
  lb_boot_lid_wait 正拿 valid 当"模块首帧"判据)。校正: feed_dp 在窗口内
  逐 DP 核对, 模块回值与预写不符打 `ui_state ERROR: DPx sent=A got=B`;
  任何 0x01 到达关窗; 500ms 没等到 → ERROR + 补发状态查询自愈。
  已知低概率误报: 整分推送抢在应答前、快速连发覆盖 —— ERROR 后紧跟
  值全对的 0x01 即误报; 烦人再上 msg_flag 配对加固。
- **APP 停止加热误进保温 —— 定性为 APP 不规范, 板子不改**(2026-07-31):
  实测 APP 停止只发 DP2=0 不带 DP10 → `lb_ble_control_has_heat_stop()`
  按协议只认显式 DP10=0 → stop_expected 没置 → 模块停了的边沿被当
  "自然完成"自动进保温并下发 24h 保温。协议 §4.1.7 明确停止加热 = DP10=0
  (且注明替代旧 5.5 停止指令),**已要求 APP 端改发 DP10=0**,板子不做
  兼容(曾加过"DP2=0 也算停止"的对齐,按决定已撤)。APP 改好前用旧写法
  停止会进保温,属预期。启动方向同理:必须带 DP10=1,只发 DP2 模块只改
  属性不启动(新 APP 启动已发对)。
- **APP 预约时间发天内秒 —— 板子打了桥层换算补丁**(2026-07-31,APP 暂无法
  改版):协议 §3.6 预约时间 = unix 秒,APP 实发本地天内秒(16:03 → 57780),
  模块按 UTC 比较差 8 小时永远不触发(定性见 §1.4 第 2 条)。补丁在
  `lb_bridge.c` 的 `lb_schedule_time_fixup()`:ADD/MODIFY 透传前,时间值
  **< 86400 视为本地天内秒**,换算成"下一次到点"的 unix(今天该时刻,已过则
  明天,−8h 回 UTC)再发;≥ 86400 视为正常 unix 原样放行 —— 屏幕自建预约
  不受影响,APP 改发 unix 后补丁自动失效。换算时打
  `lb_bridge: sched time fixup ...` 可在日志确认。
  已知代价:① APP 回读列表拿到的是 unix,它自己的时间显示可能乱(自作自受,
  APP 归正时一起消);② "仅一次" repeat=0xFF 补丁管不了,若模块按天内秒
  循环比较会天天触发,须 APP 改 repeat 语义。

联调时踩的两个坑:

1. **`OFF_DONE` 曾是死状态** —— 清它的只有 `lunchbox_shutdown_abort()`,而它
   零调用点。结果第一次关机跑完卡在 `OFF_DONE`,后续所有关机请求被
   `lunchbox_shutdown_start()` 的早退分支静默吃掉(连 APP 应答都不回),
   同时 `!is_done()` 恒假导致退出加热页不再停止加热。
   改为 `lunchbox_shutdown_done_take()` 一次性取边沿、取走即回 IDLE。
2. **关机第一步误用 `lb_heat_cmd_stop()`** —— 它是 `DP10=0` + **`DP1=1`**
   (给"停加热但不关机"用的,退出加热页就该这样),用在关机第一步等于断电前
   把总开关又打开一下,日志里能看到模块应答回 `PowerSwitch=ON`。
   已新增只发 `DP10=0` 的 `lb_heat_cmd_heat_off()` 给时序用。

### 2.5 已知 bug,暂缓修(2026-07-30 首次编译通过后发现)

编译 0 error,以下三条从警告里查出来,都不影响链接,按优先级排:

1. ~~**鸡腿/意面页温度显示成 `%F`**~~ — 已修(2026-07-30):两处 `"%%uF"` 改 `"%uF"`。

2. **保温预设时长存不下 1440** — 要先定方案。
   `lb_mode_preset_t.duration_min` 是 `u8`,`[LB_MODE_WARM] = { 5, 1440 }`
   实际存进去是 **160**(`1440 & 0xFF`)。告警
   `changed value from '1440' to '160'`。

   保温页本身不受影响(它用常量 `LB_WARM_DURATION_MIN`,不读预设表),
   但 APP 发 `0x09` 查保温预设会读到 160 分钟。

   **扩字段解决不了**:协议 `0x09/0x0a` 的时长字段就是 1 字节(上限 255),
   24 小时无法用这两条命令表达。两条路:
   - 保温那行填 `0`(协议 0 = 不限时),注释说明实际时长由
     `LB_WARM_DURATION_MIN` 决定、不走协议
   - 或协议加宽时长字段(需 APP 配合)

3. **`char buf[8]` 可能截断** — 低风险。
   `func_heat_page.c` 的 `"%uH%02uMin"`:120 分钟 → `"2H00Min"` 正好 7+1=8 不溢出;
   若哪天该页显示到 1440 分钟 → `"24H00Min"` 需要 9 字节,会被截成 `"24H00Mi"`。
   当前保温走保温页所以碰不到。buf 开到 16 即可。

---

## 3. 未移植的小件(要用时从 elunchbox 取)

- ~~`lb_get_display_tm()`~~ — 已补,见 `lb_bridge.c`。
- ~~三餐预设下发~~ — 已补,见 `lb_ble_app.c` 的 `lb_ble_send_meal_presets()`:
  时间同步经模块确认后下发早/午/晚三条(ID 1/2/3,149F,60/70/90min,
  `enabled=0` 建好不启用,`repeat=0xff` 每天),每次 BLE 连接只发一次。
  **时间点 08:00 / 10:50 / 16:30 沿用 elunchbox,午餐 10:50 待产品确认。**

---

## 4. 协议约定(待写入文档)

- **msg_flag 分段**:APP 请求限用 `0x00~0x7F`,`0x80~0xFF` 保留给 MCU 本地命令
  (`lb_heat_cmd` 自增后 `|= 0x80`)及加热模块 OTA。**尚未写入协议文档,后续
  作为修订加进《蓝牙通讯协议1.0.10.md》,并需通知 APP 端遵守**。
